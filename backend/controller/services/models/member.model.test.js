const MembersModelTest = require('./member.model');
const dbSetUP = require('../utils/mock.data').members;

describe('Member Model', () => {
    test('juest in pending',()=>{
        pending();
    })
})